*** get_ip_ranges.py ***

- modify get_ip_ranges script to receive the mininum size of the subnet and also the maximum (optional, 
can run still not if provided). For the country code, if the user wants to receive all, then accept ALL as an argument
and have the program return ranges for all countries

*** scan.py ***
- When we are scanning for all ip ranges, make sure to calculate the country that the IP address is in before
appending it to the results. This could be a simple IF statement in the insert_result function
- redo scan_results table to have the ip address and port as the primary key. if the ip and port is found again during 
our scanning, we simply replace the new results from the previous one. 


*** mc_status_check.py ***


*** FUTURE IDEAS ***

- collect ip ranges from big ISPs and servers to scan those as well since people can host their servers on there (ON HOLD)

- If active_servers.json is not empty and the user wants to refresh the list w/ players online, then have a script to 
look through the active_servers list and then update active players online list (ON HOLD)

- Have an automated script that refreshes the scanning of online servers to see if any players have joined them


*** Website ***
- adjust the number of server cards and the size of them depending on the size of the user's screen. So a computer would
show more per page
- When looking for version, sql query should include "LIKE" and then include the main release version, because there's
so many versions. We simplifiy to 1.21.x for example. example: we are looking for version 1.21.4, We select 1.21 and
1.21.4 should be included by using the LIKE clause in SQL
Example: SELECT * FROM online_servers WHERE version LIKE '1.21.%';


- truncate the dropdown for countries and list all possible countries even if there's no existing servers in that country
- set a state variable that keeps track of the current page. Reset to first page if any of the filters are modified
- reset to first page when filters are changed